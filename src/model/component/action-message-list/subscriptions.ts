
// const uid = helper.getUId();

export default {

    /**
     * 读取用户消息
     */
    // loopReadActionMessage({ dispatch, history }: SubscriptionAPI) {

    //     history.listen(({ pathname }: Location) => {
    //         if (pathname === '/default' || pathname === '/permission/law-case') {
    //             dispatch({
    //                 type: 'actionMessageList/queryMessage', payload: {
    //                     userId: uid,
    //                     state: ActionMessageState.Unread
    //                 }
    //             });
    //         }
    //     });
    // }
};