// import { SubscriptionAPI } from "dva";


export default {
    // validRoutePath({ dispatch, history }: SubscriptionAPI) {

    //     history.listen((location, action) => {

    //         const { pathname } = location;
    //         dispatch({ type: 'allowByRoutePath', payload: pathname });
    //     });
    // }
}