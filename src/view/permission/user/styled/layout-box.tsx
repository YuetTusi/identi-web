import styled from 'styled-components';

const SearchBox = styled.div`
	display: flex;
	flex-direction: row;
	justify-content: space-between;
	padding: 5px;
`;
const RoleFormBox = styled.div`
	height: 80px;
`;

export { RoleFormBox, SearchBox };
